import api=require("../../../Common/routes/user")import generate=require("./base")const req={	login:generate(api.login),	createQQ:generate(api.createQQ),	save:generate(api.save),	logout:generate(api.logout),	editPass:generate(api.editPass),	reset:generate(api.reset),	question:generate(api.question),	applyList:generate(api.applyList),	handleApply:generate(api.handleApply),	editSendInfo:generate(api.editSendInfo),	sendInfo:generate(api.sendInfo),	version:generate(api.version),}export =req