import api=require("../../../Common/routes/test")
import generate=require("./base")
const req={	editTest:generate(api.editTest),	list:generate(api.list),	info:generate(api.info),	editModule:generate(api.editModule),	editGroup:generate(api.editGroup),	removeModule:generate(api.removeModule),	removeGroup:generate(api.removeGroup),	removeTest:generate(api.removeTest),	test:generate(api.test),	editStatus:generate(api.editStatus),	editOutput:generate(api.editOutput),	move:generate(api.move),	projectList:generate(api.projectList),	editProject:generate(api.editProject),	removeProject:generate(api.removeProject),	handleApply:generate(api.handleApply),	editOwner:generate(api.editOwner),	filterList:generate(api.filterList),	editUser:generate(api.editUser),	removeUser:generate(api.removeUser),	edtUser:generate(api.edtUser),	users:generate(api.users),	quit:generate(api.quit),	interfaceList:generate(api.interfaceList),	interface:generate(api.interface),	urlList:generate(api.urlList),	interfaceProject:generate(api.interfaceProject),	editCollection:generate(api.editCollection),	collectionList:generate(api.collectionList),	collection:generate(api.collection),	removeCollection:generate(api.removeCollection),	cooperation:generate(api.cooperation),	editCooperation:generate(api.editCooperation),	allGroupList:generate(api.allGroupList),	allList:generate(api.allList),	exportProject:generate(api.exportProject),	importProject:generate(api.importProject),	exportModule:generate(api.exportModule),	importModule:generate(api.importModule),	exportGroup:generate(api.exportGroup),	importGroup:generate(api.importGroup),	exportTest:generate(api.exportTest),	importTest:generate(api.importTest),	pasteTest:generate(api.pasteTest),}
export =req