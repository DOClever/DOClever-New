import api=require("../../../Common/routes/team")
import generate=require("./base")
const req={	save:generate(api.save),	info:generate(api.info),	pullUser:generate(api.pullUser),	removeUser:generate(api.removeUser),	removeProjectUser:generate(api.removeProjectUser),	projectUser:generate(api.projectUser),	editUserRole:generate(api.editUserRole),	moveUser:generate(api.moveUser),	editGroup:generate(api.editGroup),	removeGroup:generate(api.removeGroup),	pullProject:generate(api.pullProject),	userApply:generate(api.userApply),	projectApply:generate(api.projectApply),	group:generate(api.group),	editNotice:generate(api.editNotice),	notice:generate(api.notice),	removeNotice:generate(api.removeNotice),	applyInfo:generate(api.applyInfo),	editApply:generate(api.editApply),	removeProject:generate(api.removeProject),	userPulledList:generate(api.userPulledList),	userJoin:generate(api.userJoin),	remove:generate(api.remove),	transfer:generate(api.transfer),	user:generate(api.user),	list:generate(api.list),	projectList:generate(api.projectList),	docList:generate(api.docList),	testList:generate(api.testList),	removeDoc:generate(api.removeDoc),	docUser:generate(api.docUser),	pullDoc:generate(api.pullDoc),	docApply:generate(api.docApply),	removeTest:generate(api.removeTest),	testUser:generate(api.testUser),	pullTest:generate(api.pullTest),	testApply:generate(api.testApply),}
export =req