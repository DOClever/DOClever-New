import api=require("../../../Common/routes/project")
import generate=require("./base")
const req={	create:generate(api.create),	editMemeber:generate(api.editMemeber),	editRole:generate(api.editRole),	removeMember:generate(api.removeMember),	list:generate(api.list),	filterList:generate(api.filterList),	editUrl:generate(api.editUrl),	info:generate(api.info),	group:generate(api.group),	interface:generate(api.interface),	clear:generate(api.clear),	remove:generate(api.remove),	quit:generate(api.quit),	addUrl:generate(api.addUrl),	exportJSON:generate(api.exportJSON),	exportHTML:generate(api.exportHTML),	importJSON:generate(api.importJSON),	inject:generate(api.inject),	urlList:generate(api.urlList),	importMemberInfo:generate(api.importMemberInfo),	editImportMemeber:generate(api.editImportMemeber),	editOwner:generate(api.editOwner),	applyList:generate(api.applyList),	handleApply:generate(api.handleApply),	editUser:generate(api.editUser),	users:generate(api.users),	importRap:generate(api.importRap),	importSwagger:generate(api.importSwagger),	updateSwagger:generate(api.updateSwagger),	importPostman:generate(api.importPostman),	exportDoc:generate(api.exportDoc),}
export =req